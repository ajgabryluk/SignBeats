using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Unity.UI;

public class SignController : MonoBehaviour
{
    // public DotController dotController;
    // public List<int> signPositions;
    // public GameObject[] signs;
    // public Sprite[] highlightedSigns;
    // public Sprite[] completedSigns;
    // public List<GameObject> instantiatedObjects = new List<GameObject>();
    // [SerializeField] Transform canvas;

    void Start()
    {
        // int signPositionIndex = 0;
        // for(int i = 0; i < dotController.dots.Length; i++)
        // {
        //     if(signPositions.Contains(i))
        //     {
        //         Vector3 spawnPosition = dotController.dots[signPositions[signPositionIndex]].transform.position;
        //         GameObject newObject = Instantiate(signs[0], spawnPosition, Quaternion.identity, canvas);
        //         instantiatedObjects.Add(newObject);
        //         signPositionIndex++;
        //     }
        //     else
        //     {
        //         instantiatedObjects.Add(null);
        //     }
        // }
    }
}
